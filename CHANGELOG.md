# 變更紀錄

本書的結構演進。記錄的重點不是「加了什麼」，而是**為什麼加**——
因為每一次擴充，都是因為前一版在某個真實處境下不夠用。

---

## 0.4　第八篇：更複雜的情況

**加了什麼**：第 46–49 章（共病與診斷遮蔽、感官與內感受、重度與無口語的另一條路徑、女兒）、
附錄 E（家庭與三十年）、附錄 F（術語表）、附錄 G（來源與延伸閱讀）。

**為什麼**：前四十五章寫的是一個**條件相當好**的孩子——自理獨立、情緒可控、有可用的固著引擎。
現場更常見的不是這樣。第 48 章把整本書翻譯成另一條路徑（把「獨立程度」換成「參與程度」）；
第 46 章補上「所有方法都試過就是沒用」的第三種解釋（他身上有第二個沒被診斷的問題）；
第 49 章補上以男性為預設所漏掉的那一半。

附錄 G 處理的是這本書自己的矛盾：一本要求別人標註證據等級的書，卻沒有給出任何來源。

**同時新增的檢查**：術語表一致性（列出的詞必須在正文出現）、交叉引用（「第 N 章」必須存在）。
兩者上線時各抓到一個真實的錯——術語表列了正文沒有的 PECS、附錄 F 提到生酮飲食卻沒帶免責框。

**同時完成的交付物**：简体版（`cn/`，機械轉換）與兩版 PDF（各 25 MB）。
產出過程踩到兩個新的坑，都已回寫進 BUILD_STANDARD §8：
`.bat` 含中文會被 cmd.exe 以 ANSI 頁碼解析成亂碼、以及 Chrome 150 會 exit 0 但不產生檔案（Edge 可以）。
而 OpenCC 把全書「核心」轉成「内核」335 處，是「機械轉換不是翻譯」最具體的一次示範。

---

## 0.3　第七篇：情境百景

**加了什麼**：第 41–45 章的五十個情境、附錄 A 的情境總索引。

**為什麼**：方法書教「怎麼做」，但家長真正的處境是「他現在站在超商門口不肯走，後面排了五個人」。
原本的書要求他先讀完三章再自己組合——而情境不給你這個時間。
五十景各自標了對應章節，可以只讀一景。

**同時新增的檢查**：情境七段骨架（五十景用同一副骨架，寫到第三十七景一定會漏段——所以讓機器數）、
情境索引一致性（正文標題與附錄索引必須逐字相同）。後者上線時抓到解析器自己的 bug：
誤把附錄裡另一張同格式的表格當成情境索引。

---

## 0.2　第六篇：專業補強

**加了什麼**：第 35–40 章（行為功能評估、工作分析與連鎖、增強的技術、青春期與身體界線、
危機處理、十八歲的法律斷崖）。

**為什麼**：前五篇給的策略全部預設了一組行為功能（逃避與獲得）。
第 35 章補上「先問這個行為在替他做什麼」——若功能是感官，代幣與處罰完全無效。
第 36 章補上多步驟技能的教法（「他學不會折衣服」其實是十一件事被當成一件事在教）。
第 38、40 章補上兩塊最常被迴避、而代價全部由孩子承擔的內容。

**工具**：新增 `abc` 與 `function` 指令；工作分析步驟卡。

---

## 0.1　初版：五篇 34 章

**內容**：認識這顆獨特的大腦、三大核心、六大階段、教材與工具箱、轉銜與永續，
加上附錄 A–D。零依賴的 `railway_core` 工具包、`_build.py` 建置鏈、
書稿與資產的雙重檢查、變異測試。

**核心命題**：不是把火車從他手裡拿走，而是讓火車載他去上班。

---

## 品保的演進

| 版本 | 書稿檢查 | 資產檢查 | 變異測試 |
|------|---------|---------|---------|
| 0.1 | 6 項 | 6 項 | 18 個變異 |
| 0.2 | 6 項 | 6 項 | 23 個變異 |
| 0.3 | 7 項 | 7 項 | 23 個變異 |
| 0.4 | **9 項** | **7 項** | **26 個變異** |

變異測試的三次「首跑存活」，每一次都指出一個沒人在看的行為：

1. **0.1**：7 個存活——包含 `all([])` 是 True、`< 3` 寫成 `<= 3`、沒量到的指標當成通過。
2. **0.2**：1 個存活——測試用 `MIN_RECORDS_FOR_HYPOTHESIS - 1` 構造測資，
   **用常數自己測那個常數**；把門檻從 10 改成 1，測試照樣通過。
3. **0.4**：0 個存活（記錄表的三個邊界一次到位）。

---

## 已知未完成

見**附錄 G 第六節**（這本書已知的限制）。最主要的三項：

- 未經醫師、律師、行為分析師或特教教師的正式審閱；
- 未經家長試讀驗證；
- ~~`impl/` 的 pytest 套件未實際執行~~ → **已解除**：透過 Windows 端的 Python 跑完整套，
  pytest 160 項全過、覆蓋率 95.3%、mypy strict 零錯誤、ruff 修掉 15 項（含一個真實缺陷：
  表格產生器的 `zip()` 未加 `strict=`，欄數不符時會靜默截斷一欄）。
